BEGIN {
	FS = " -> "
	# set-up the grid
}

/^.+/ {
	#loop through $1 - $NF and put # in each grid spot that joins them
	for (r = 1; r < $NF; r++) {
		split($r, start, ",")
		split($(r+1), end, ",")
		
		if (start[1] == end[1]) {
			
		} else {
		
		}
	}
}

END {

}